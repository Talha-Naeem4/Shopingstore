<%= link_to 'back', home_path %>

Products



  <% @products.each do |product| %>
    
<%= link_to product.title, "https: #{@current_shopify_session.shop} admin products #{product.id}", target: "_top" %>
  <% end %>